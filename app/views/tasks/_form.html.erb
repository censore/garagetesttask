<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :taskdeadline %><br>
    <%= f.datetime_select :taskdeadline %>
  </div>
  <div class="field">
    <%= f.label :parentid %><br>
    <%= f.number_field :parentid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
